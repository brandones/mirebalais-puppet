#!/bin/sh
#
# Script to clean up the Reports that we run nightly on the production server
#

DIR=/home/<%= @tomcat %>/.OpenMRS/reports
FILENAME=mirebalaisreports*zip

# Delete all reports more than 15 days old
find ${DIR}/${FILENAME} -mtime +15 -exec rm {} \;


